import React from 'react';
import { Box, Typography } from '@mui/material';

export default function TicketsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" sx={{ fontWeight: 600 }}>Tickets (Placeholder)</Typography>
      <Typography variant="body1">Ticket list will be shown here.</Typography>
    </Box>
  );
}
